import { defineConfig } from 'vite'

export default defineConfig({
  // Thay 'TEN-REPO-CUA-BAN' bằng tên repository thực tế của bạn trên GitHub
  // Ví dụ: repo của bạn là 'IOT' thì sửa thành base: '/IOT/'
  // Nếu bạn deploy lên trang cá nhân username.github.io thì xóa dòng base này đi.
  base: '/TEN-REPO-CUA-BAN/',
})
